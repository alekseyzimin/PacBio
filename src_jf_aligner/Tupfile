include_rules

export PKG_CONFIG_PATH

CXXFLAGS += -I.. $(JELLYFISH_CFLAGS) -I../include
LDFLAGS += $(JELLYFISH_LIBS)

: jf_aligner_cmdline.yaggo |> !yaggo |>
: foreach jf_aligner.cc pb_aligner.cc superread_parser.cc | jf_aligner_cmdline.hpp |> !cxx |> {jf_aligner}
: {jf_aligner} |> !lxxd |> jf_aligner
