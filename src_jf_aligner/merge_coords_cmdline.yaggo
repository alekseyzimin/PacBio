purpose "Merge coords file generated by jf_aligner"

description "Merge coords file. It is expected that the order of the
query sequences is identical in all input files and all query
sequences are present, even if no match is reported. This requires
switches --compact and --zero-match to jf_aligner."

option("o", "output") {
  description "Output file (stdout)"
  c_string; typestr "PATH" }
arg("coords") {
  description "Coord file to merge"
  c_string; typestr "PATH"
  multiple }
